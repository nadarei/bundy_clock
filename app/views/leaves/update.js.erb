$(document).trigger('close.facebox');
$('tr#<%= row_id(@time_log.date, current_user) %>')
  .find('td.leave')
  .html(
      <%= render(partial: 'leave',
        locals: { time_log: @time_log }).to_json.html_safe %>);
